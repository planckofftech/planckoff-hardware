# Supabase Development : software.planckoff@gmail.com
# Your Supabase project URL (safe to expose to the browser)
NEXT_PUBLIC_SUPABASE_URL=

# Supabase anonymous/public key (safe to expose to the browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=



NEXT_PUBLIC_APP_URL=


# Supabase service role key — SERVER ONLY, never prefix with NEXT_PUBLIC_
# Used for admin operations (bypasses RLS). Keep this secret.
SUPABASE_SERVICE_ROLE_KEY=


# AI providers — server-only (never exposed to browser)
OPENROUTER_API_KEY=